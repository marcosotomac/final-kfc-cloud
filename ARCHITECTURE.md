# 🏗️ Arquitectura AWS - KFC Orders System

## 📊 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CAPA DE PRESENTACIÓN                                │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                          Next.js Frontend (my-app)                        │  │
│  │                                                                            │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │  Admin   │  │ Cocina   │  │ Empaque  │  │ Delivery │  │ Register │  │  │
│  │  │   /Caja  │  │  Worker  │  │  Worker  │  │  Worker  │  │  Tenant  │  │  │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  │  │
│  └───────┼─────────────┼─────────────┼─────────────┼─────────────┼────────┘  │
└──────────┼─────────────┼─────────────┼─────────────┼─────────────┼───────────┘
           │             │             │             │             │
           │             └─────────────┴─────────────┴─────────────┘
           │                           │
           ▼                           ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CAPA DE API GATEWAY                                 │
│                                                                                  │
│  ┌──────────────────────────┐          ┌──────────────────────────────────┐    │
│  │   API Gateway (HTTP)     │          │   API Gateway (WebSocket)        │    │
│  │   2afuwya2yc.execute-api │          │   hmvtmrvzu4.execute-api         │    │
│  │                          │          │                                  │    │
│  │  POST   /tenants         │          │  $connect    → wsConnect         │    │
│  │  POST   /tenants/{id}/   │          │  $disconnect → wsDisconnect      │    │
│  │         orders           │          │  $default    → wsDefault         │    │
│  │  GET    /tenants/{id}/   │          │  ping        → wsPing            │    │
│  │         orders           │          │                                  │    │
│  │  GET    /tenants/{id}/   │          └──────────────┬───────────────────┘    │
│  │         orders/{orderId} │                         │                        │
│  └──────────────┬───────────┘                         │                        │
└─────────────────┼─────────────────────────────────────┼────────────────────────┘
                  │                                     │
                  ▼                                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            CAPA DE LÓGICA DE NEGOCIO                             │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                        AWS Lambda Functions                              │   │
│  │                                                                           │   │
│  │  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐               │   │
│  │  │registerTenant │  │ createOrder   │  │  listOrders   │               │   │
│  │  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘               │   │
│  │          │                  │                  │                         │   │
│  │  ┌───────┴───────┐  ┌───────┴───────┐  ┌───────┴───────┐               │   │
│  │  │   getOrder    │  │orderEventsRtr │  │  wsConnect    │               │   │
│  │  └───────────────┘  └───────┬───────┘  └───────────────┘               │   │
│  │                              │                                           │   │
│  │  ┌───────────────┐  ┌────────┴────────┐  ┌───────────────┐            │   │
│  │  │ wsDisconnect  │  │   wsDefault     │  │    wsPing     │            │   │
│  │  └───────────────┘  └─────────────────┘  └───────────────┘            │   │
│  │                                                                          │   │
│  │  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐              │   │
│  │  │kitchenWorker  │  │packagingWorker│  │deliveryWorker │              │   │
│  │  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘              │   │
│  └──────────┼──────────────────┼──────────────────┼──────────────────────┘   │
└─────────────┼──────────────────┼──────────────────┼──────────────────────────┘
              │                  │                  │
              ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         CAPA DE ORQUESTACIÓN Y MENSAJERÍA                        │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                    AWS Step Functions - OrderWorkflow                     │  │
│  │                                                                            │  │
│  │     ┌─────────────┐      ┌─────────────┐      ┌─────────────┐           │  │
│  │     │SendToKitchen│ ───► │SendToPacking│ ───► │SendToDeliver│           │  │
│  │     │   (Task)    │      │   (Task)    │      │   (Task)    │           │  │
│  │     └──────┬──────┘      └──────┬──────┘      └──────┬──────┘           │  │
│  │            │                    │                    │                    │  │
│  │            │ waitForTaskToken   │ waitForTaskToken   │ waitForTaskToken  │  │
│  │            ▼                    ▼                    ▼                    │  │
│  └────────────┼────────────────────┼────────────────────┼───────────────────┘  │
│               │                    │                    │                       │
│  ┌────────────▼────────┐  ┌────────▼────────┐  ┌───────▼────────┐            │
│  │   SQS - Kitchen     │  │ SQS - Packaging │  │ SQS - Delivery │            │
│  │       Queue         │  │      Queue      │  │     Queue      │            │
│  │  (VT: 120s)         │  │   (VT: 120s)    │  │  (VT: 120s)    │            │
│  └─────────────────────┘  └─────────────────┘  └────────────────┘            │
│                                                                                 │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │              EventBridge - OrdersEventBus                                 │  │
│  │              (kfc-orders-cloud-bus-dev-v5)                                │  │
│  │                                                                            │  │
│  │  ┌─────────────────────┐         ┌──────────────────────────────┐        │  │
│  │  │ Event Pattern:      │         │  Targets:                    │        │  │
│  │  │ - order.created     │────────►│  1. OrderWorkflow (SFN)      │        │  │
│  │  │ - order.updated     │         │  2. orderEventsRouter (λ)    │        │  │
│  │  │ - order.completed   │         │                              │        │  │
│  │  └─────────────────────┘         └──────────────────────────────┘        │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                 │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                    SNS - OrderNotificationsTopic                          │  │
│  │              (kfc-orders-cloud-notifications-dev-v5)                      │  │
│  │                                                                            │  │
│  │  Publica notificaciones de cambios de estado de órdenes                  │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            CAPA DE PERSISTENCIA                                  │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                         DynamoDB Tables                                   │  │
│  │                                                                            │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐     │  │
│  │  │ TenantsTable (kfc-orders-cloud-tenants-dev-v5)                  │     │  │
│  │  │ ─────────────────────────────────────────────────────────────── │     │  │
│  │  │ PK: tenantId                                                     │     │  │
│  │  │ Attributes: name, location, createdAt                           │     │  │
│  │  │ BillingMode: PAY_PER_REQUEST                                    │     │  │
│  │  └─────────────────────────────────────────────────────────────────┘     │  │
│  │                                                                            │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐     │  │
│  │  │ OrdersTable (kfc-orders-cloud-orders-dev-v5)                    │     │  │
│  │  │ ─────────────────────────────────────────────────────────────── │     │  │
│  │  │ PK: tenantId | SK: orderId                                      │     │  │
│  │  │ Attributes: status, items[], customerName, createdAt, history[] │     │  │
│  │  │ GSI: status-index (tenantId + status)                           │     │  │
│  │  │ BillingMode: PAY_PER_REQUEST                                    │     │  │
│  │  └─────────────────────────────────────────────────────────────────┘     │  │
│  │                                                                            │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐     │  │
│  │  │ ConnectionsTable (kfc-orders-cloud-connections-dev-v5)          │     │  │
│  │  │ ─────────────────────────────────────────────────────────────── │     │  │
│  │  │ PK: tenantId | SK: connectionId                                 │     │  │
│  │  │ Attributes: role, expiresAt                                     │     │  │
│  │  │ GSI: tenant-role-index (tenantId + role)                        │     │  │
│  │  │ GSI: connection-index (connectionId + tenantId)                 │     │  │
│  │  │ TTL: expiresAt (Enabled)                                        │     │  │
│  │  │ BillingMode: PAY_PER_REQUEST                                    │     │  │
│  │  └─────────────────────────────────────────────────────────────────┘     │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                    S3 - MediaBucket                                       │  │
│  │              (kfc-orders-cloud-media-dev-v5)                              │  │
│  │                                                                            │  │
│  │  • CORS habilitado para upload de imágenes                               │  │
│  │  • Public Access Bloqueado                                               │  │
│  │  • Uso: Almacenamiento de imágenes de productos/recibos                 │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                            CAPA DE SEGURIDAD Y IAM                               │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                    IAM Role: LabRole                                      │  │
│  │                    ARN: arn:aws:iam::595645243021:role/LabRole           │  │
│  │                                                                            │  │
│  │  Permisos para:                                                           │  │
│  │  • Lambda Execution                                                       │  │
│  │  • DynamoDB Read/Write                                                    │  │
│  │  • SQS Send/Receive/Delete                                               │  │
│  │  • SNS Publish                                                            │  │
│  │  • EventBridge PutEvents                                                  │  │
│  │  • Step Functions StartExecution/SendTaskSuccess                         │  │
│  │  • API Gateway Invoke                                                     │  │
│  │  • CloudWatch Logs                                                        │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Trabajo Completo

### 1️⃣ Registro de Tenant (Franquicia)
```
Usuario (Frontend) 
    │
    └──► POST /tenants
         │
         └──► Lambda: registerTenant
              │
              └──► DynamoDB: TenantsTable
                   │
                   └──► Response: { tenantId, name, location }
```

### 2️⃣ Creación de Orden
```
Admin (Frontend)
    │
    └──► POST /tenants/{tenantId}/orders
         │
         └──► Lambda: createOrder
              │
              ├──► DynamoDB: OrdersTable (Guardar orden)
              │
              └──► EventBridge: PutEvent
                   │
                   └──► Event: order.created
                        │
                        ├──► Target 1: Step Functions (OrderWorkflow)
                        │    │
                        │    └──► Inicia workflow de 3 etapas
                        │
                        └──► Target 2: Lambda (orderEventsRouter)
                             │
                             ├──► API Gateway WebSocket
                             │    │
                             │    └──► Notifica a clientes conectados
                             │
                             └──► SNS: OrderNotificationsTopic
```

### 3️⃣ Workflow de Procesamiento (Step Functions)
```
OrderWorkflow (Step Functions)
    │
    ├──► Step 1: SendToKitchen
    │    │
    │    └──► SQS: KitchenQueue
    │         │
    │         └──► Lambda: kitchenWorker
    │              │
    │              ├──► Simula preparación en cocina
    │              ├──► DynamoDB: Actualiza status → "kitchen"
    │              ├──► EventBridge: PutEvent (order.updated)
    │              └──► SendTaskSuccess (continúa workflow)
    │
    ├──► Step 2: SendToPackaging
    │    │
    │    └──► SQS: PackagingQueue
    │         │
    │         └──► Lambda: packagingWorker
    │              │
    │              ├──► Simula empaque
    │              ├──► DynamoDB: Actualiza status → "packaging"
    │              ├──► EventBridge: PutEvent (order.updated)
    │              └──► SendTaskSuccess (continúa workflow)
    │
    └──► Step 3: SendToDelivery
         │
         └──► SQS: DeliveryQueue
              │
              └──► Lambda: deliveryWorker
                   │
                   ├──► Simula entrega
                   ├──► DynamoDB: Actualiza status → "completed"
                   ├──► EventBridge: PutEvent (order.completed)
                   └──► SendTaskSuccess (finaliza workflow)
```

### 4️⃣ Conexión WebSocket en Tiempo Real
```
Worker (Frontend)
    │
    └──► WebSocket: $connect
         │
         └──► Lambda: wsConnect
              │
              ├──► DynamoDB: ConnectionsTable
              │    │
              │    └──► Guarda { tenantId, connectionId, role, expiresAt }
              │
              └──► Response: 200 OK

orderEventsRouter (al recibir eventos)
    │
    └──► DynamoDB: ConnectionsTable (Query por tenantId + role)
         │
         └──► API Gateway WebSocket
              │
              └──► Envía mensaje a conexiones activas del tenant
```

---

## 📊 Estadísticas de Recursos

### Lambda Functions: **12**
- registerTenant
- createOrder, listOrders, getOrder
- orderEventsRouter
- wsConnect, wsDisconnect, wsDefault, wsPing
- kitchenWorker, packagingWorker, deliveryWorker

### DynamoDB Tables: **3**
- TenantsTable
- OrdersTable (+ 1 GSI)
- ConnectionsTable (+ 2 GSIs)

### SQS Queues: **3**
- KitchenQueue
- PackagingQueue
- DeliveryQueue

### Otros Recursos:
- **1** Step Functions State Machine
- **1** EventBridge Event Bus
- **1** SNS Topic
- **1** S3 Bucket
- **1** API Gateway HTTP
- **1** API Gateway WebSocket

---

## 🔐 Configuración de Seguridad

### CORS (HTTP API)
```yaml
allowedOrigins: ["*"]
allowedHeaders: [Content-Type, Authorization, X-Requested-With, 
                 x-tenant-id, x-user-id, x-role]
allowedMethods: [OPTIONS, GET, POST, PATCH, PUT, DELETE]
```

### TTL en Conexiones
- Las conexiones WebSocket expiran después de 3600 segundos (1 hora)
- Renovables mediante acción `ping`

---

## 🌐 Endpoints de Producción

### HTTP API
```
Base URL: https://2afuwya2yc.execute-api.us-east-1.amazonaws.com

POST   /tenants
POST   /tenants/{tenantId}/orders
GET    /tenants/{tenantId}/orders
GET    /tenants/{tenantId}/orders/{orderId}
```

### WebSocket API
```
WSS URL: wss://hmvtmrvzu4.execute-api.us-east-1.amazonaws.com/dev

Actions:
- $connect
- $disconnect
- $default
- ping
```

---

## 🚀 Escalabilidad y Rendimiento

### Auto-scaling
- ✅ Lambda: Escalado automático (hasta 1000 instancias concurrentes)
- ✅ DynamoDB: PAY_PER_REQUEST (escalado bajo demanda)
- ✅ API Gateway: Sin límite de requests configurado

### Timeouts
- Lambda Functions: 10-30 segundos
- SQS Visibility Timeout: 120 segundos
- WebSocket Connection TTL: 3600 segundos

### Límites de Memoria
- Lambda Functions: 1024 MB

---

## 📈 Monitoreo y Logs

### CloudWatch
- Logs de todas las Lambda functions
- Métricas de Step Functions
- Alarmas de SQS (DLQ)
- Trazas de API Gateway

---

## 🏷️ Multi-Tenancy

El sistema está diseñado para soportar múltiples franquicias (tenants):

- Cada tenant tiene su propio `tenantId`
- Aislamiento de datos por tenant en DynamoDB
- Conexiones WebSocket segregadas por tenant
- Eventos filtrados por tenant en EventBridge

---

## 📦 Despliegue

```bash
# Deploy completo
serverless deploy

# Deploy de función específica
serverless deploy function -f createOrder

# Remover stack
serverless remove
```

---

## 🔍 Tecnologías Utilizadas

**Backend:**
- AWS Lambda (Python 3.11)
- AWS Step Functions
- Amazon DynamoDB
- Amazon SQS
- Amazon SNS
- Amazon EventBridge
- AWS API Gateway (HTTP + WebSocket)
- Amazon S3

**Frontend:**
- Next.js 16
- React 19
- TypeScript
- Tailwind CSS

**IaC:**
- Serverless Framework 4.25.0

---

## 📝 Notas de Arquitectura

1. **Patrón Event-Driven**: Uso extensivo de EventBridge para desacoplar componentes
2. **Patrón Saga**: Step Functions coordina el workflow multi-etapa
3. **Patrón CQRS**: Separación de escrituras (EventBridge) y lecturas (DynamoDB GSI)
4. **Real-time Updates**: WebSocket para notificaciones instantáneas
5. **Serverless**: Arquitectura 100% serverless, pago por uso
